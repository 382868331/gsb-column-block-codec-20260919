# 完整首轮任务

请从零实现“可随机定位的列式整数块编码库”，用于数据工程 / 列存格式中的本地可复用模块。

1. 从零实现 int64或None 序列的二进制列块库，每块最多128行；格式自定义并版本化，不实现Parquet兼容。文件含行数、块目录、每块空值位图与载荷，所有整数元数据编码及端序在README精确定义。
2. 为非空值支持三种块编码：原始int64、delta+ZigZag位打包、连续值RLE；delta预测只沿非空值推进，用Python整数处理可能超int64的差值。按实际序列化长度选择最短编码，平局按上述顺序。
3. 位打包使用块统一位宽，支持0位宽和65位宽；不足整字节的尾位必须为0。区分空列、全空块与常数块，解码每个最终值必须仍在int64范围。
4. 提供Encode/Decode、ReadRange(start,stop)和块统计；范围读取只解码相交块，统计实际解码块数。加载目录时校验偏移、边界、重叠及总行数；每个被读取块用CRC32验证，未触及块校验可延迟但必须明确。
5. 拒绝截断、非法位宽、行计数不符、RLE长度不符、非零padding和解压超过声明行数；设置总行数与字节数上限。用直接列表切片作固定种子往返与随机范围参考。

验收重点：int64最小最大交替、None间隔、128行边界、单行、全空、常数RLE、坏目录、坏CRC、65位delta。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Python 3.14.7标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；python demo.py须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为python -m unittest discover -s tests -v。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。
