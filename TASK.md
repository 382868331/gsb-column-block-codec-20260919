# 完整首轮任务

需要把一列可空整数编码成数据块，并能只解码查询范围涉及的块。请实现列式整数编解码库，按下面的格式处理空值、压缩方式和校验。

实现int64或None序列列块格式，每块最多128行，只有末块可不足；含版本、总行数、连续无空洞块目录、空值位图和载荷，精确端序及字段布局在README给出，不兼容Parquet。

三种非空值编码：原始int64；首个非空值原始int64、后续相邻非空值delta经ZigZag统一位宽打包；对非空值序列的连续值RLE。按完整块实际字节数选择最短，平局按上述顺序。

delta用Python整数，ZigZag(d)=2d若d>=0，否则-2d-1，位宽最小并允许0..65；未使用尾位为0。非空值不足2时无delta项，全空块不存首值；还原值须在int64内。

实现Encode/Decode、ReadRange(start,stop)，须0<=start<=stop<=总行数，空范围解码0块。加载时校验目录连续、边界和行数，读取时仅解码相交块并记录数量；每块CRC32覆盖该块编码标识、空值位图及载荷，未读块校验可延迟。

构造时限定最大行数和输入字节，解码不能超声明行数；拒绝截断、非法位宽、坏CRC、RLE长度不符和非零padding。用短列表参考核验往返及随机范围，演示128行边界与65位delta。

测试里请覆盖这些情况：int64两端交替、None间隔RLE、128边界、全空/常数/单行、65位delta、目录空洞、CRC、空范围和越界。

使用 Python 3.14.7 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `python demo.py` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `python -m unittest discover -s tests -v` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。
