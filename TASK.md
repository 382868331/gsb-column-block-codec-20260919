# 完整首轮任务

离线分析组件要压缩带空值整数列，并只读取命中的小块。请从零实现“可随机定位的列式整数块编码库”。

1. 实现int64或None序列列块格式，每块最多128行，只有末块可不足；含版本、总行数、连续无空洞块目录、空值位图和载荷，精确端序及字段布局在README给出，不兼容Parquet。
2. 三种非空值编码：原始int64；首个非空值原始int64、后续相邻非空值delta经ZigZag统一位宽打包；对非空值序列的连续值RLE。按完整块实际字节数选择最短，平局按上述顺序。
3. delta用Python整数，ZigZag(d)=2d若d>=0，否则-2d-1，位宽最小并允许0..65；未使用尾位为0。非空值不足2时无delta项，全空块不存首值；还原值须在int64内。
4. 实现Encode/Decode、ReadRange(start,stop)，须0<=start<=stop<=总行数，空范围解码0块。加载时校验目录连续、边界和行数，读取时仅解码相交块并记录数量；每块CRC32覆盖该块编码标识、空值位图及载荷，未读块校验可延迟。
5. 构造时限定最大行数和输入字节，解码不能超声明行数；拒绝截断、非法位宽、坏CRC、RLE长度不符和非零padding。用短列表参考核验往返及随机范围，演示128行边界与65位delta。

验收重点：int64两端交替、None间隔RLE、128边界、全空/常数/单行、65位delta、目录空洞、CRC、空范围和越界。

工程约束：全新0-1核心库，仅用Python 3.14.7标准库，在Windows原生离线运行，无第三方依赖、外部服务、Docker或前后端。交付实现、聚焦上述边界的测试及简短README；演示命令`python demo.py`用小案例在约8秒内打印正常结果和一种真实失败，测试命令`python -m unittest discover -s tests -v`。固定种子随机验证只用少量小样本，避免长压测和额外平台功能，不限制正常纠错所需工具调用。
一次首轮完成，不要求用户追加“继续”。只操作当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区；不添加Actions/CI/部署/Dependabot、不远程推送，不硬编码演示或虚报验证。
