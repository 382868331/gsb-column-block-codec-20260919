# 可随机定位的列式整数块编码库

当前为0-1任务的共同初始骨架，尚未实现业务功能。完整需求见 TASK.md。

## 环境与依赖

Windows原生 Python 3.14.7；仅标准库，无第三方安装步骤、外部服务、账号或密钥。

## 命令

演示：`python demo.py`

测试：`python -m unittest discover -s tests -v`

初始演示仅明确打印骨架提示，初始无业务测试；命令可启动不代表题目实现或验收通过。模型须补充真实实现、测试及短演示。

## 范围

数据工程 / 列存格式。空值与差分预测、位打包、块索引和损坏数据验证必须保持随机读取与全量解码一致。

不设置工具调用次数上限，不执行网络或后台任务。禁止GitHub自动执行；不提交凭据、配置或运行轨迹。
