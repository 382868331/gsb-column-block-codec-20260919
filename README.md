# 可随机定位的列式整数块编码库

离线分析组件要压缩带空值整数列，并只读取命中的小块。

当前是0-1共同初始骨架，没有业务实现或参考答案。完整规格见TASK.md，API命名及模块组织可自行选择，必须满足其语义。

## 环境与命令

Windows原生Python 3.14.7，仅标准库，无第三方依赖安装步骤，无账号、密钥、外部服务或Docker。

演示：`python demo.py`

实现后测试：`python -m unittest discover -s tests -v`

初始演示只输出SCAFFOLD ONLY。初始测试尚未编写；Python 3.14运行空测试集会显示NO TESTS RAN并返回5，这是空骨架状态，不需为此反复排障或添加占位测试。模型需补充真实库、测试和约8秒演示。

## 实现范围

空值与差分预测、位打包、块索引和损坏数据验证必须保持随机读取与全量解码一致。

只实现TASK.md要求的核心，不增加UI、网络服务或长压力测试。演示保留真实失败，测试可正常纠错；不要为了减少步骤跳过验证。不得提交凭据、配置或轨迹，不启用GitHub自动执行。
